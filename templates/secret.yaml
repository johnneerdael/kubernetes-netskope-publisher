{{- if and (not (include "npa-publisher.isApiEnrollment" .)) (not .Values.registrationToken.existingSecret) .Values.registrationToken.value }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "npa-publisher.fullname" . }}-token
  labels:
    {{- include "npa-publisher.labels" . | nindent 4 }}
type: Opaque
stringData:
  token: {{ .Values.registrationToken.value | quote }}
{{- end }}
